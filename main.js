const name = hannah

if ( name.split('').join() ==== name.split('').reverse()

// split hannah into individual letters.
// Reverse the spelling of hannah
// If reversed Hannah is the same as original Hannah then the name Hannah is a
// a palindrome.

const isPalindrome (hannah)
